# MiniGit-Git-like-Version-Control-System
A Git-inspired version control system built in C++ featuring commits, branching, merging, and file diffs. Uses Merkle Trees for content integrity, DAGs for commit history management, hashing for object storage, and the LCS algorithm for efficient diff computation.
